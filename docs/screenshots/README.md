# Screenshots

Screenshots used by the repository root README.

Current files:

- `overview.png` — the complete PageCraft preview and Harness layout.
- `element-selection.png` — an active DOM selection with its feedback editor.
- `feedback-queue.png` — multiple queued comments with the right-side scrollbar visible.
