# Screenshots

Add public, non-sensitive screenshots here.

Recommended files:

- `overview.png` — the complete PageCraft view with preview, selected element, feedback editor, and Harness composer.
- `element-selection.png` — hover highlight and selected DOM element.
- `feedback-queue.png` — several queued comments with the right-side scrollbar visible.

For `overview.png`, use a 16:9 or wide desktop capture, remove private conversation names and local paths, and keep text readable at the GitHub README width. After adding it, uncomment the image line in the repository root `README.md`.
